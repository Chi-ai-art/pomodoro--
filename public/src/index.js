import React from 'react';
import ReactDOM from 'react-dom/client';
import PomodoroTimer from './PomodoroTimer';  // ←いま貼ってるファイル名と合わせる

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <PomodoroTimer />
);
